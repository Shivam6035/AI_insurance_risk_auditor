# name: CI/CD Pipeline for Insurance Auditor

# # 1. Define when the workflow should trigger
# on:
#   push:
#     branches: [ "main" ]
#   pull_request:
#     branches: [ "main" ]

# # 2. Set environment variables used across jobs
# env:
#   PYTHON_VERSION: "3.11"
#   IMAGE_NAME: ${{ github.repository }}

# jobs:
#   # ==========================================
#   # CI: Testing Job
#   # ==========================================
#   test:
#     name: Run Unit and Integration Tests
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Python ${{ env.PYTHON_VERSION }}
#         uses: actions/setup-python@v5
#         with:
#           python-version: ${{ env.PYTHON_VERSION }}
#           cache: 'pip' # Automatically caches dependencies to speed up future runs

#       - name: Install Dependencies
#         run: |
#           python -m pip install --upgrade pip
#           pip install -r requirements.txt
#           # If you use linting tools like flake8 or black, install them here

#       - name: Run Pytest
#         # We set dummy environment variables so Pydantic's BaseSettings doesn't crash during tests
#         env:
#           OPENAI_API_KEY: "dummy-key-for-testing"
#           TAVILY_API_KEY: "dummy-key-for-testing"
#         run: |
#           pytest -v tests/

#   # ==========================================
#   # CD: Docker Build and Push Job
#   # ==========================================
#   build-and-push:
#     name: Build and Push Docker Image
#     needs: test # This ensures the image is ONLY built if the test job passes
#     runs-on: ubuntu-latest
    
#     # Only push the Docker image when code is merged/pushed directly to main, not on PRs
#     if: github.event_name == 'push' && github.ref == 'refs/heads/main'

#     permissions:
#       contents: read
#       packages: write # Required to push to GitHub Container Registry

#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Log in to the GitHub Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Extract metadata (tags, labels) for Docker
#         id: meta
#         uses: docker/metadata-action@v5
#         with:
#           images: ghcr.io/${{ env.IMAGE_NAME }}
#           tags: |
#             type=raw,value=latest
#             type=sha,format=short

#       - name: Build and push Docker image
#         uses: docker/build-push-action@v5
#         with:
#           context: .
#           push: true
#           tags: ${{ steps.meta.outputs.tags }}
#           labels: ${{ steps.meta.outputs.labels }}
#           # Utilizes Docker layer caching to speed up consecutive builds
#           cache-from: type=registry,ref=ghcr.io/${{ env.IMAGE_NAME }}:buildcache
#           cache-to: type=registry,ref=ghcr.io/${{ env.IMAGE_NAME }}:buildcache,mode=max